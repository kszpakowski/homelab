apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argo-workflows
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
      - workflows.homelab.kszpakowski.com
      secretName: grafana-cert
  rules:
  - host: "workflows.homelab.kszpakowski.com"
    https:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argo-server
            port:
              name: web

